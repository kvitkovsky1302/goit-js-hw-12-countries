<div>
    <h2>{{name}}</h2>
    <p>Capital: {{capital}}</p>
    <p>Population: {{population}}</p>
    <ul>Languages:
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
    <img src="{{flag}}" alt="{{name}}">
</div>